@model ChangePersonalInformationViewModel
@{
    ViewData["Title"] = "Wijzig persoonlijke gegevens";
}

<h2>@ViewData["Title"].</h2>

<form asp-controller="Manage" asp-action="ChangePersonalInformation" method="post" class="form-horizontal">
    <h4>Persoonlijke Informatie</h4>
    <hr />
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <label asp-for="Birthday" class="col-md-2 control-label">Geboortedatum</label>
        <div class="col-md-10">
            <input asp-for="Birthday" class="form-control datetimepicker" type="text" placeholder="dd-mm-yyyy" />
            <span asp-validation-for="Birthday" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Sex" class="col-md-2 control-label">Geslacht</label>
        <div class="col-md-10">
            <select asp-for="Sex" asp-items="Html.GetEnumSelectList<Sex>()" class="form-control"></select>
            <span asp-validation-for="Sex" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <label asp-for="Iban" class="col-md-2 control-label">Iban</label>
        <div class="col-md-10">
            <input asp-for="Iban" class="form-control" />
            <span asp-validation-for="Iban" class="text-danger"></span>
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn-default">Wijzig</button>
        </div>
    </div>
</form>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }

    <script type="text/javascript">
        $(function () {
            $('.datetimepicker').datetimepicker({
                locale: 'nl',
                format: 'DD-MM-YYYY'
            });
        });
    </script>
}
